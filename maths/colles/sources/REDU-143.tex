\begin{enumerate}
\item Soit $M$ une matrice diagonale d'ordre $n$ dont les coefficients diagonaux sont deux à deux distincts. Montrer que les matrices commutant avec $M$ sont exactement les matrices diagonales.
\item Déterminer les valeurs propres de $A=\begin{pmatrix}1 & 3 & 0 \\ 3 & -2 & -1 \\ 0 & -1 & 1\end{pmatrix}$.
\item Combien y a-t-il de matrice $M$ telle que $M^2=A$ dans $\mathscr{M}_n(\mathbb{C})$ ? dans $\mathscr{M}_n(\mathbb{R})$ ?                                                                                                                          \end{enumerate}
