Trouver toutes les formes linéaires $f$ sur $\mcal M_n(\mathbb K)$ vérifiant :
\begin{equation*}
    \forall\,A,B \in \mcal M_n(\K),~ f(AB) = f(BA).
\end{equation*}
\emph{Indication :} pour deux matrices élémentaires $E_{a,b}$ et $E_{c,d}$, calculer le produit $E_{a,b}E_{c,d}$.
