\begin{enumerate}% CCP 2017, ex. 9
\item Soit $X$ un ensemble, $(g_n)$ une suite de fonctions de $X$ dans \C\ et $g$ une fonction de $X$ dans \C.\\
Donner la définition de la convergence uniforme de la suite de fonctions $(g_n)$ vers la fonction $g$.
\item On pose $f_n(x)=\dfrac{n+2}{n+1}\e^{-nx^2}\cos(x\sqrt{n})$.
\begin{enumerate}
\item Étudier la convergence simple de la suite de fonctions $(f_n)$.
\item La suite de fonctions $(f_n)$ converge-t-elle uniformément sur $[0,\pinf[$ ?
\item Soit $a>0$. La suite de fonctions $(f_n)$ converge-t-elle uniformément sur $[a,\pinf[$ ?
\item La suite de fonctions $(f_n)$ converge-t-elle uniformément sur $]0,\pinf[$ ?
\end{enumerate}

\end{enumerate}
